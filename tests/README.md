# tests

Automated test suite, runnable with a single command.

No tests exist yet — TASK-001 only establishes the application shell, which
has no business logic worth testing. Per the project testing strategy,
coverage will be prioritized for: database/repositories, core business
logic, AI response validation, import/export, process generation, and
critical native functionality.
