# services

Frontend-side wrappers around Tauri `invoke` calls. The React layer talks to
Rust exclusively through these services — never through direct filesystem,
database, or native API access.

Not yet populated — no Tauri commands beyond the foundation health check
exist yet.
