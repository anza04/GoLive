# features

Feature-scoped modules (e.g. projects, captures, processes, settings), each
owning its own components, hooks, and local state.

Not yet populated — introduced starting with the Projects feature (M1).
