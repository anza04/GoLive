# components

Small, reusable, presentation-focused UI building blocks with no
feature-specific business logic (buttons, inputs, cards, layout primitives).

Not yet populated — introduced as the UI grows in later tasks.
